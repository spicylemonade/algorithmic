"""Geometry and scattering calculations."""


def compute_observing_geometry(orbital_state, epoch):
    return {"phase_angle_deg": 0.0, "aspect_angle_deg": 0.0}


def render_flux(shape_state, spin_state, geometry):
    return 1.0
