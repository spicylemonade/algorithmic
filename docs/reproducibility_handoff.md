# Reproducibility and Handoff Package

## Manifest
- Configs: `results/item_017_experiment_matrix.json`, `docs/training_curriculum_stabilization.md`
- Seeds: global base seed `42`; alternate reproducibility seeds `142, 242, 342`
- Datasets: self-play proxy trajectories generated by `src/alphago5d/selfplay.py`
- Checkpoints: `results/checkpoints/checkpoint_manifest.json`
- Metrics logs: `results/metrics_log_schema_example.jsonl`
- Figures: `figures/item_021_scaling_curves.png`

## Command Recipes
1. Run baseline benchmark protocol:
   `PYTHONPATH=src python3 - <<'PY'\nfrom alphago5d.agents import RandomAgent, HeuristicAgent, ShallowSearchAgent\nfrom alphago5d.evaluation import play_match\nprint(play_match(ShallowSearchAgent(2), HeuristicAgent(), games=1000, seed=42))\nPY`
2. Run self-play throughput check:
   `PYTHONPATH=src python3 - <<'PY'\nfrom alphago5d.selfplay import SelfPlayPipeline\nprint(SelfPlayPipeline(workers=4, seed=42).run_games(200))\nPY`
3. Reproduce scaling artifacts:
   `python3 results/rebuild_item_021_scaling.py` (recipe captured in rubric history command logs).

## Headline Results to Recreate
- 1000-game superiority benchmarks (`item_019`)
- Ablation Elo deltas with CI (`item_018`)
- Robustness cap compliance (`item_020`)
- Scaling and efficiency curves (`item_021`)
